source "https://rubygems.org"

# Matches the gems GitHub Pages uses to build the site, so local previews
# behave like production. Run `bundle exec jekyll serve` to preview.
gem "github-pages", group: :jekyll_plugins
